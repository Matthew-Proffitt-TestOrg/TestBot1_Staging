{
  "name": "@org/trading-bot",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "docs": "turbo run docs",
    "coverage": "turbo run coverage",
    "prepare": "echo 'husky placeholder'"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.6.3"
  }
}